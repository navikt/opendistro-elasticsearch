{{ if .Values.odfe.istio.inject }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ template "fullname" . }}-devnav
  labels:
    app: {{ template "fullname" . }}
    team: {{ .Values.odfe.env.TEAM }}
spec:
  gateways:
  - istio-system/ingress-gateway-dev-nav-no
  hosts:
  - {{ template "fullname" . }}.dev-nav.no
  http:
  - route:
    - destination:
        host: {{ template "fullname" . }}
        port:
          number: 9200
        subset: ""
      weight: 100
{{ end }}
